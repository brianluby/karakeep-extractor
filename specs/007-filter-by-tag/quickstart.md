# Quickstart: Filtering

## Filter by Tag

To see only repositories related to a specific topic:

```bash
karakeep rank --tag "python"
```

You can combine this with other flags:

```bash
karakeep rank --tag "cli" --sort stars --limit 5
```
